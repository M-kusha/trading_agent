execution:
  max_workers: 4
  retry_attempts: 3
  timeout: 30
monitoring:
  health_check_interval: 30
  log_level: DEBUG
  metrics_interval: 60
system:
  environment: development
  name: Trading Agent System
  version: 1.0.0
