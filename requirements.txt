# AI Trading System - PPO-Only Requirements
#
# Every package listed here is imported by tracked source. Verified by import scan
# on 2026-08-02; unused declarations were removed rather than carried forward.
#
# Install:   pip install -r requirements.txt
# CUDA:      pip install torch --index-url https://download.pytorch.org/whl/cu121
# Python >= 3.10 required. MetaTrader5 requires Windows.

# ── Core numerics ────────────────────────────────────────────────────────────
# Upper bounds are deliberately open: the codebase runs on numpy 2.x / pandas 3.x.
# The previous "<2.0" / "<3.0" pins contradicted the working environment and would
# have silently downgraded a functioning install.
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3.0

# ── Reinforcement learning ───────────────────────────────────────────────────
torch>=2.0.0
stable-baselines3>=2.0.0
sb3-contrib>=2.0.0          # MaskablePPO - required by the action-mask design
gymnasium>=0.28.0
optuna>=3.5.0

# ── Backend / dashboard services ─────────────────────────────────────────────
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
pydantic>=2.0.0
starlette>=0.27.0
aiohttp>=3.8.0

# ── Broker integration ───────────────────────────────────────────────────────
MetaTrader5>=5.0.40

# ── Utilities ────────────────────────────────────────────────────────────────
pyyaml>=6.0.0
psutil>=5.9.0
pytz>=2023.3
matplotlib>=3.7.0

# ── Development / test ───────────────────────────────────────────────────────
pytest>=7.4.0

# ── Removed, and why ─────────────────────────────────────────────────────────
# sqlite3           stdlib module; `pip install -r` fails hard on this line
# ta-lib            not imported anywhere; needs a separate binary toolchain
# yfinance          not imported anywhere
# numba, h5py       not imported anywhere
# seaborn, plotly   not imported anywhere
# structlog, rich   not imported anywhere
# memory_profiler   not imported anywhere
# pympler           not imported anywhere
# httpx             not imported anywhere
# cryptography      not imported anywhere
# toml              not imported anywhere
# python-dotenv     not imported anywhere
# python-dateutil   not imported directly (transitive via pandas)
# requests          not imported anywhere
# aiofiles          not imported anywhere
# websockets        not imported directly (transitive via uvicorn[standard])
# tensorboard       not imported directly; add back when TB logging is wired up
# scipy             not imported by tracked source (only by diagnostics)
# flake8/black/mypy dev tooling - belongs in a separate requirements-dev.txt
